# Xs5 v1.0.0

首个正式版本。

- 固定国家 SOCKS5 出口池，同国家支持多个独立出口。
- VPN Gate / Proxio / 全部来源，多候选自动切换和健康检查。
- 节点延迟、出口响应、出口 IP 属性、ISP / ASN 展示。
- 新版 Web UI 与交互。
- 新增 `xs5` 管理菜单、管理密码重置、更新、修复和卸载。
- 一键安装支持 IP 直连、80 端口 HTTP-01 HTTPS、Cloudflare DNS-01 HTTPS。
- S5 地址始终保持服务器公网 IP。
- 支持从旧 Country S5 Pool / X S5 Pool 数据目录迁移。

> Xs5 使用第三方公开 VPN / SOCKS5 节点。公开节点可能不稳定、不可信或被目标站封禁。请阅读仓库 README 中的完整风险与免责声明，不要通过不受信任的公共出口传输敏感明文数据。
