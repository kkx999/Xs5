# Xs5 v1.3.5

本版本修复 v1.3.4 上传测速达到 8 秒主动停止时，被误判为 `context canceled` 测速失败的问题。

- 下载与上传测速统一改用明确的 Context Deadline 作为 8 秒时间上限，不再通过“已传输耗时是否接近 8 秒”推测是否属于正常停止。
- 当测速达到设定时间上限时，只要已经获得足够样本数据，就按正常测速完成并计算速度，不再误报“上传测速中断”。
- 真正的网络异常、HTTP 错误和样本过小仍会正确返回测速失败。
- 同一出口 10 秒重测冷却、下载/上传各最多约 50 MB、MB/s 展示方式保持不变。
- 测速仍与健康检查、FailCount、自动切换、自学习评分和动态冷却完全隔离。
- 不改变 VPN Gate / Proxio / ProxyScrape 节点选择、自动恢复和既有 S5 配置。
